## Requirements

- Standard Linux environment
- POSIX shell (`/bin/sh` or `bash`)
- A C++ compiler with C++20 support (e.g. `g++`)
- Nvidia GPU device with CC >= 7.0
- Installed CUDA drivers

## Build Instructions

From the project root directory, run:

```bash
chmod +x ./build.sh && ./build.sh
```